<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="maint.list">
      <field name="name">maint list</field>
      <field name="model">maint.maint</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="maint.action_window">
      <field name="name">maint window</field>
      <field name="res_model">maint.maint</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="maint.action_server">
      <field name="name">maint server</field>
      <field name="model_id" ref="model_maint_maint"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="maint" id="maint.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="maint.menu_1" parent="maint.menu_root"/>
    <menuitem name="Menu 2" id="maint.menu_2" parent="maint.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="maint.menu_1_list" parent="maint.menu_1"
              action="maint.action_window"/>
    <menuitem name="Server to list" id="maint" parent="maint.menu_2"
              action="maint.action_server"/>
-->
  </data>
</odoo>